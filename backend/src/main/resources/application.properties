# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smart_inventory?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework=INFO
logging.level.com.inventory=DEBUG

# Application Name
spring.application.name=Smart Inventory Management System

# ============================================
# EMAIL CONFIGURATION - GMAIL SMTP
# ============================================

# Enable email sending
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anvithaboppana999@gmail.com
spring.mail.password=qukw dswv oyfm ilnm

# SMTP Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email From Address
app.email.from=YOUR_EMAIL@gmail.com

# Frontend URL for reset link
app.frontend.url=http://localhost:8080

# ============================================
# IMPORTANT: Replace these values:
# 1. YOUR_EMAIL@gmail.com - Your Gmail address
# 2. YOUR_APP_PASSWORD - Gmail App Password (NOT your regular password)
# 
# How to get App Password:
# 1. Go to Google Account Settings
# 2. Security → 2-Step Verification (enable it)
# 3. Security → App passwords
# 4. Generate password for "Mail" app
# 5. Copy the 16-character password
# ============================================
